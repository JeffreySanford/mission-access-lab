spring:
  datasource: { url: jdbc:h2:mem:testdb;MODE=PostgreSQL, username: sa, password: '' }
  jpa: { hibernate: { ddl-auto: create-drop } }
  flyway: { enabled: false }
app:
  security: { enabled: false }
  openfga: { base-url: http://localhost:8082, store-id: '', model-id: '', request-timeout: 2s }
